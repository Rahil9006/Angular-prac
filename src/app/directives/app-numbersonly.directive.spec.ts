import { AppNumbersonlyDirective } from './app-numbersonly.directive';

describe('AppNumbersonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNumbersonlyDirective();
    expect(directive).toBeTruthy();
  });
});
