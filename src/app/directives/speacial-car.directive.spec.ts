import { SpeacialCarDirective } from './speacial-car.directive';

describe('SpeacialCarDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeacialCarDirective();
    expect(directive).toBeTruthy();
  });
});
